# PoplarPheno-Collector 开发日志

## 项目概述

一款用于杨树及其他木本植物表型数据野外采集的 Android App，支持标准化拍照、GPS定位、元数据管理、JSON/CSV批量导出。

**最终成功版本：** `PoplarPheno-Collector-v9.apk` (7.8MB)

---

## 版本演化

| 版本 | 技术栈 | 结果 | 原因 |
|------|--------|------|------|
| v1/v2 (Expo/RN) | React Native + Expo SDK 54 | ❌ 手机运行失败 | Hermes JS 引擎兼容/架构问题 |
| v3-v6 (Expo) | React Native + Expo 托管 | ❌ 构建/运行失败 | Expo 托管工作流限制，原生模块冲突 |
| v7-v8 (Expo bare) | React Native + 原生混合 | ❌ 安装后闪退/无响应 | RN 与原生 CameraX 生命周期冲突 |
| Native v1 | Kotlin + Compose + CameraX | ⚠️ 初版成功但简陋 | 功能不足 |
| **Native v2** | Kotlin + Compose + CameraX | ✅ **首个稳定版** | 纯原生，无 RN 包袱 |
| **v9 (当前)** | Kotlin + Compose + CameraX + Room + SAF | ✅ **功能完整版** | 在 v2 基础上全面扩展 |

---

## v9 完整架构

```
PoplarPheno-Collector-v9/
├── app/src/main/java/com/pheno/collector/
│   ├── MainActivity.kt              # 入口 + 底部导航（项目/拍摄/数据）
│   ├── data/
│   │   ├── local/
│   │   │   ├── AppDatabase.kt       # Room 数据库单例
│   │   │   ├── ProjectDao.kt        # 项目 DAO
│   │   │   └── PhotoDao.kt          # 照片 DAO
│   │   └── model/
│   │       ├── Project.kt           # 项目实体 + CameraMode/ReferenceType 枚举
│   │       └── PhotoRecord.kt       # 照片实体 + ProjectExport DTO
│   ├── ui/
│   │   ├── MainViewModel.kt         # 全局状态管理（AndroidViewModel）
│   │   ├── camera/
│   │   │   ├── CameraScreen.kt      # 拍照界面（含模式/参照物/GPS/表单）
│   │   │   └── CameraController.kt  # CameraX 生命周期管理
│   │   ├── project/
│   │   │   └── ProjectScreen.kt     # 项目创建/选择界面
│   │   └── data/
│   │       └── DataScreen.kt        # 数据管理 + 导出界面
│   └── util/
│       ├── FileNamingEngine.kt      # 文件命名规范 + 路径管理
│       ├── ExportEngine.kt          # JSON/CSV/照片批量导出
│       └── LocationProvider.kt      # GPS 定位管理（FusedLocation）
└── app/src/main/res/
    ├── drawable/                    # 自适应图标矢量素材
    ├── mipmap-*/                    # 各密度图标 PNG
    └── xml/file_paths.xml           # FileProvider 路径配置
```

---

## 关键技术决策与教训

### ⚠️ 为什么不继续用 React Native / Expo

1. **Expo 托管工作流** 有很多原生模块限制，出问题难以调试
2. **Hermes 引擎字节码编译** 导致 APK 缺少 JS bundle，Release 包无法运行
3. **CameraX + RN 生命周期冲突** 是根本性架构问题，不是代码 bug
4. **纯原生 (Kotlin + Compose)** 编译快、调试快、APK 小（7.8MB vs RN 的 50+MB）
5. 原生 Android 不需要构建 JavaScript 桥接层，可靠性远超 RN

### ⚠️ Android 存储权限陷阱（重点！）

**问题：** 直接写 `/sdcard/PhenoCollector/` 在 Android 10+ 不可用
- Android 10 (API 29) 引入了分区存储（Scoped Storage）
- `Environment.getExternalStorageDirectory()` 已废弃
- 写入失败表现：`fail to write temp file`

**解决方案：**
```kotlin
// ❌ 错误 - Android 10+ 不可用
val dir = File(Environment.getExternalStorageDirectory(), "PhenoCollector")

// ✅ 正确 - 使用 App 私有目录，无需任何权限
val dir = File(context.filesDir, "PhenoCollector")
```

**照片可见性：** 通过 `MediaStore` 插入到 `DCIM/PoplarPheno`，用户可在相册查看

### ⚠️ CameraX 拍照回调线程安全（致命 bug！）

**问题：** 拍照立即闪退
- CameraX 的 `takePicture()` 第二个参数是 `Executor`，我们在 CameraController 中用了 `Executors.newSingleThreadExecutor()`
- `ImageCapture.OnImageSavedCallback` 的回调在这个后台线程执行
- 在回调中直接调用 `Toast.makeText()`、`ContentResolver.insert()` → **直接从后台线程操作 UI/系统API → 闪退**

**解决方案：**
```kotlin
// CameraController 中使用 mainExecutor 包装回调
fun takePicture(outputOptions: ..., onResult: (Boolean, ImageCaptureException?) -> Unit) {
    imageCapture?.takePicture(outputOptions, executor, object : ImageCapture.OnImageSavedCallback {
        override fun onImageSaved(output: ...) {
            mainExecutor.execute { onResult(true, null) }  // ← 主线程回调
        }
        override fun onError(exc: ...) {
            mainExecutor.execute { onResult(false, exc) }
        }
    })
}
```

### ⚠️ Room 数据库必须用 withContext(Dispatchers.IO)

所有 DAO 操作（非 `@Query(...) Flow<...>` 的）必须包装在 IO 协程中：
```kotlin
withContext(Dispatchers.IO) {
    db.photoDao().getPhotoCountByProject(project.id)
}
```
否则会触发主线程数据库访问异常。

### ⚠️ SAF (Storage Access Framework) 注意事项

- `ActivityResultContracts.OpenDocumentTree()` 返回的 URI 需要 `takePersistableUriPermission()` 才能持久访问
- `DocumentFile.createFile("image/jpeg", name)` 创建照片文件时 MIME 类型必须正确
- `DocumentFile.createDirectory()` 返回可能为 null，必须判空
- 需要添加依赖：`androidx.documentfile:documentfile:1.0.1`

---

## 文件命名规范

### 照片命名：`YYYYMMDD_<MODE>_<PROJECTID>_<INDEX>.jpg`
- 示例：`20260518_CLOSEUP_PRJ001_001.jpg`
- MODE: `CLOSEUP` / `DISTANCE` / `MICROSCOPE`
- INDEX: 三位补零 (001, 002...)

### 项目编号：`PRJ` + 三位序号
- 示例：`PRJ001`, `PRJ002`

### 导出结构：
```
PRJ001_export/
├── PRJ001_manifest.json    # 完整元数据
├── PRJ001_photos.csv       # 照片清单
├── PRJ001_README.txt       # 说明文件
└── photos/
    ├── 20260518_CLOSEUP_PRJ001_001.jpg
    └── ...
```

---

## 构建命令

```bash
cd /home/ian/database/apkforphenotype/PoplarPheno-Collector-v9
./gradlew clean assembleRelease
# APK: app/build/outputs/apk/release/app-release.apk
```

**签名信息：**
- Keystore: `app/pheno-release.keystore`
- 密码: `pheno123`
- 别名: `pheno`
- 包名: `com.pheno.collector`

---

## 依赖清单

```kotlin
// Compose BOM 2024.06.00
// CameraX 1.3.4
// Room 2.6.1 + KSP
// play-services-location 21.3.0  (GPS)
// accompanist-permissions 0.34.0
// documentfile 1.0.1  (SAF)
// compileSdk 35, minSdk 24, targetSdk 35
```

---

## 未来可扩展方向

1. **WiFi/HTTP 直接上传服务器** - 替代 USB 拷贝
2. **图片压缩选项** - 可调 JPG quality (当前 `CAPTURE_MODE_MAXIMIZE_QUALITY`)
3. **离线地图标记** - 在地图上查看采集点位
4. **批量照片预览** - 缩略图网格浏览
5. **参照物自动识别** - 用 OpenCV 检测参照物尺寸
6. **暗色/亮色模式切换** - 当前仅深色森林主题

---

## 开发教训总结

1. **React Native ≠ 万能** — 涉及 Camera/GPS/文件系统等原生能力的 App，纯原生 Kotlin 更可靠
2. **先测试最简单路径** — Native v1 → v2 证明了纯原生方案可行，花了很长时间才得到这个结论
3. **权限不是越多越好** — 改成 App 私有目录不需要任何权限，反而更稳定
4. **回调线程是高频 bug 来源** — 任何第三方 SDK 的回调都必须假设在非主线程运行
5. **Room 和文件夹是数据核心** — 数据安全 > UI 美观，先保证数据可靠存储和导出
